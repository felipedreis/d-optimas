package settings;

import br.cefetmg.lsi.bimasco.settings.RegionSettings;

public class SimulationSettingsTests {
/*
    SimulationSettingsMock simulationSettingsMock;

    @Before
    public void setUp() {
        simulationSettingsMock = ConfigFactory.create(SimulationSettingsMock.class);
    }

    @Test
    public void simulationSettingsNotNull() {
        assertNotNull(simulationSettingsMock);
        assertNotNull(simulationSettingsMock.region());
        assertNotNull(simulationSettingsMock.agents());
        assertNotNull(simulationSettingsMock.problem());
    }

    @Test
    public void simulationSettingsCorrectParser() {
        assertEquals("", "Some description", simulationSettingsMock.description());
        assertEquals("", "1", simulationSettingsMock.type());
        assertEquals("", false, simulationSettingsMock.hasCooperation());
    }

    @Test
    public void regionSettingsCorrectParser() {

        RegionSettings defaultSettings = new RegionSettings();
        defaultSettings.setCentroid(0);
        defaultSettings.setMaxValueFitnessFunction(99999999.0);
        defaultSettings.setMinValueFitnessFunction(0.0);
        defaultSettings.setTotalSumOfSolutions(0.0);

        assertEquals(defaultSettings.getCentroid(), simulationSettingsMock.region().getCentroid(), 0.01);
        assertEquals(defaultSettings.getMaxValueFitnessFunction(), simulationSettingsMock.region().getMaxValueFitnessFunction(), 0.01);
        assertEquals(defaultSettings.getMinValueFitnessFunction(), simulationSettingsMock.region().getMinValueFitnessFunction(), 0.01);
        assertEquals(defaultSettings.getTotalSumOfSolutions(), simulationSettingsMock.region().getTotalSumOfSolutions(), 0.01);
    }
    */
}
