package core.agents;

import br.cefetmg.lsi.bimasco.core.agents.Agent;
import br.cefetmg.lsi.bimasco.settings.AgentSettings;
import br.cefetmg.lsi.bimasco.settings.SimulationSettings;

public class AgentTests {
/*
    private SimulationSettings simulationSettings;
    private AgentSettings agentSettings;
    private boolean hasCooperation;
    private Agent agent;

    @Before
    public void givenProblemSettingsInstance() throws IllegalAccessException, ClassNotFoundException, InstantiationException {
        SimulationSettingsMock simulationSettingsMock = ConfigFactory.create(SimulationSettingsMock.class);
        this.simulationSettings = simulationSettingsMock;
        this.agentSettings = simulationSettingsMock.agents()[0];
        this.hasCooperation = simulationSettingsMock.hasCooperation();
    }

    @Before
    public void whenICreatedAnAgent() throws IllegalAccessException, ClassNotFoundException, InstantiationException {
        this.agent = new Agent(simulationSettings, agentSettings);
    }

    @Test
    public void then(){

    }
*/
}
